// import {useElements,useSpeed} from "../SortVisualizer"
// import { Element } from "../../types/Types";

// export async function BubbleSort(elements: Element[]) {
//     for (let index = 0; index < elements.length - 1; index++) {
//         for (let index = 0; index < elements.length - 1; index++) {
//             await swap(elements, index, index + 1, elements[index].value > elements[index + 1].value);
//         }
//     }
// return elements;
// }
// function sleep(ms: number) {
//     return new Promise(resolve => setTimeout(resolve, ms));
// }
// const swap = async (array: Element[], first: number, second: number, condition: boolean) => {
//     array[first].color = "green";
//     array[second].color = "green";
    
//     setElements(elements.slice());
//     await sleep(speed)
//     if (condition) {
//         array[first].color = "red";
//         array[second].color = "red";
//         setElements(elements.slice());
//         await sleep(speed)
//         let temp = array[first];
//         array[first] = array[second];
//         array[second] = temp;
//         setElements(elements.slice());
//         setElements(elements)
//     }
//     array[first].color = "pink";
//     array[second].color = "pink";
//     await sleep(speed)
//     setElements(elements.slice());

// }
export function BubbleSort(){
    return 0;
}